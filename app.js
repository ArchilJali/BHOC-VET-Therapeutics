(()=>{const menu=document.querySelector('.menu-toggle');const nav=document.getElementById('mobile-nav');if(menu&&nav){menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));menu.setAttribute('aria-label',open?'Open navigation':'Close navigation');nav.classList.toggle('open',!open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false');menu.setAttribute('aria-label','Open navigation')}))}const track=document.getElementById('species-track');const prev=document.querySelector('[data-species-prev]');const next=document.querySelector('[data-species-next]');if(track&&prev&&next){const step=()=>Math.min(track.clientWidth*.78,440);prev.addEventListener('click',()=>track.scrollBy({left:-step(),behavior:'smooth'}));next.addEventListener('click',()=>track.scrollBy({left:step(),behavior:'smooth'}))}const tabs=[...document.querySelectorAll('[data-science-tab]')];const panels=[...document.querySelectorAll('[data-science-panel]')];const activate=name=>{tabs.forEach(tab=>{const active=tab.dataset.scienceTab===name;tab.classList.toggle('active',active);tab.setAttribute('aria-selected',String(active));tab.tabIndex=active?0:-1});panels.forEach(panel=>{const active=panel.dataset.sciencePanel===name;panel.classList.toggle('active',active);panel.hidden=!active})};tabs.forEach((tab,index)=>{tab.addEventListener('click',()=>activate(tab.dataset.scienceTab));tab.addEventListener('keydown',event=>{if(!['ArrowLeft','ArrowRight'].includes(event.key))return;event.preventDefault();const i=event.key==='ArrowRight'?(index+1)%tabs.length:(index-1+tabs.length)%tabs.length;tabs[i].focus();activate(tabs[i].dataset.scienceTab)})});if(tabs.length)activate('heme')})();