import {esc,lines,attrs,button,img,icon,section} from '../lib/html.mjs';
const initials=text=>String(text).split(/\s+/).map(word=>word==='BHOC'?'<strong>BH<span class="oxygen-initial">O</span>C</strong>':`<span class="hero-name-word"><strong${word==='Oxygen'?' class="oxygen-initial"':''}>${esc(word[0])}</strong>${esc(word.slice(1))}</span>`).join(' ');
export default d=>section(d,'hero',`<div class="hero-visual">${img(d.image,'fetchpriority="high" class="hero-art"')}</div><a class="initiative-mark" ${attrs(d.initiative.link)} aria-label="${esc(d.initiative.link.label)}">${img(d.initiative.image,'fetchpriority="high"')}<strong>${esc(d.initiative.title)}</strong><span class="initiative-tagline">${esc(d.initiative.tagline)}</span><span class="initiative-promise">${esc(d.initiative.promise)}</span><span class="initiative-link">${esc(d.initiative.link.label)} ${icon('arrow')}</span></a><div class="hero-copy"><h1 id="${d.id}-heading">${lines(d.heading)}</h1><p class="hero-subheading">${initials(d.subheading)}</p>${d.description?.length?`<p class="hero-intent">${lines(d.description)}</p>`:''}<div class="hero-actions">${d.actions.map(button).join('')}</div></div>`);
