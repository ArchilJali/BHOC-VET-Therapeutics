import {esc,attrs,icon,img,section} from '../lib/html.mjs';
export default d=>section(d,'species',`<div class="section-topline"><h2 class="eyebrow" id="${d.id}-heading"><a ${attrs(d.allLink)}>${esc(d.title)}</a></h2><a class="text-link" ${attrs(d.allLink)}>${esc(d.allLabel)} ${icon('arrow')}</a></div><div class="species-carousel"><button class="round-control species-prev" aria-label="Previous species" aria-controls="species-track">${icon('chevron')}</button><div class="species-track" id="species-track" tabindex="0" aria-label="Species. Swipe or use the arrow keys to explore.">${d.items.map(s=>`<a class="species-card" ${attrs(s)} ${s.href===`#species-${s.id}`?`data-species="${esc(s.id)}"`:""}><span class="portrait">${img(s.image,'loading="lazy"')}</span><h3>${esc(s.name)}</h3><p>${esc(s.description)}</p></a>`).join('')}</div><button class="round-control species-next" aria-label="Next species" aria-controls="species-track">${icon('chevron')}</button></div><p class="sr-only" id="species-announcement" aria-live="polite"></p>`);
