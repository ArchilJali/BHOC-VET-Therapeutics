import {esc,attrs,icon,section} from '../lib/html.mjs';
export default d=>section(d,'biodiversity',`<h2 class="eyebrow" id="${d.id}-heading"><a ${attrs(d.link)}>${esc(d.title)}</a></h2><div class="stats-grid">${d.stats.map(s=>`<a class="stat" ${attrs(s)}><span class="stat-prefix">${esc(s.prefix)||'&nbsp;'}</span><strong>${esc(s.value)}</strong><span>${esc(s.label)}</span>${icon(s.icon)}</a>`).join('')}<p class="stat-manifesto">${d.manifesto.map(s=>`<span>${esc(s)}</span>`).join('')}</p></div><details class="data-note"><summary>Sources and scope</summary><p>${esc(d.sourceNote)}</p></details>`);
