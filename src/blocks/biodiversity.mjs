import {esc,attrs,icon,section} from '../lib/html.mjs';
export default d=>section(d,'biodiversity',`<h2 class="eyebrow" id="${d.id}-heading"><a ${attrs(d.link)}>${esc(d.title)}</a></h2><div class="stats-grid">${d.stats.map(s=>`<article class="stat"><a class="stat-source" ${attrs(s)} aria-label="${esc(s.sourceLabel)}">${icon(s.icon)}<span class="sr-only">${esc(s.sourceLabel)}</span></a><span class="stat-prefix">${esc(s.prefix)||'&nbsp;'}</span><strong>${esc(s.value)}</strong><span>${esc(s.label)}</span></article>`).join('')}<aside class="blood-message" aria-labelledby="blood-message-heading"><p class="blood-message-eyebrow">${esc(d.message.eyebrow)}</p><h3 id="blood-message-heading">${esc(d.message.title)}</h3><p class="blood-message-copy">${esc(d.message.text)}</p><p class="blood-message-note">${esc(d.message.note)}</p><a class="blood-message-link" ${attrs(d.message.link)}>${esc(d.message.link.label)} ${icon('arrow')}</a><blockquote><p>“${esc(d.message.quote)}”</p><cite>${esc(d.message.attribution)}</cite></blockquote></aside></div><details class="data-note"><summary>Sources and scope</summary><p>${d.sources.map(source=>`<a ${attrs(source)}>${esc(source.label)}</a>: ${esc(source.description)}.`).join(' ')} ${esc(d.sourceNote)}</p></details>`);
